#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main- print if number is positive, zero or negative
 * Return: Always (Success)
 *
 */
int main(void)

{
	int low;

	for (low = 'a'; low <= 'z'; low++)
	putchar(low);
	for (low = 'a'; low <= 'z'; low++)
	putchar(low);
	putchar('\n');
	return (0);
}
